# 보증금지킴 (deposit-guard)

전세 계약 직전 세입자가 등기부등본 PDF와 보증금을 넣으면, 에이전트가 LH 권리분석 담당자처럼 무엇을 확인할지 정해 도구를 부르고 "위험한가"와 "그래서 뭘 준비해야 하나"를 의견서 한 장으로 낸다. 원티드 AI Championship 2026 출품작. 제출 마감 2026-09-20, 링크 유지 2026-09-21~10-17.

## 명세가 진실이다

`docs/specs/` 아래 싱크독 명세 체인(프로젝트 코드 `JSD`)이 원본이다. 구현 전에 해당 단계 문서를 읽는다.

| 단계 | 문서 | 무엇 |
|---|---|---|
| RFQ | `01-RFQ/JSD-RFQ-001.md` | 요구 Q1~Q39. 검토 항목 전체 목록은 Q37 |
| PRD | `02-PRD/JSD-PRD-001.md` | 기능 R1~R14, 비기능 N1~N5. **판정 규칙 R5·R6, 건물별 필수 검토 R11** |
| SCN | `03-SCN/JSD-SCN-001.md` | 시나리오 S1~S4 = 예시 파일 3건 명세 |
| UC | `04-UC/JSD-UC-001.md` | 사용자 목표 UC-A1~A4, 도구·루프 UC-S1~S10 |
| INFRA | `05-INFRA/JSD-INFRA-001.md` | 스택·배포·데이터 보관 |

명세 수정은 싱크독(MCP `syncdoc`)으로만 한다. `get_document` → `update_document`. 상태 변경은 웹에서만.

## 기술 스택 (2026-09-15 결정)

| 구분 | 선택 | 한 줄 사유 |
|---|---|---|
| 백엔드 | Python 3.12, FastAPI, uv | 도메인 아키텍처 규칙 보유, SSE·백그라운드 태스크 |
| DB | PostgreSQL (compose 컨테이너), SQLAlchemy 2 async + asyncpg, Alembic | 의뢰인 결정. 캐시도 테이블로 |
| 프론트 | React 18 + TypeScript + Vite + Tailwind. 빌드 산출물을 FastAPI가 정적 서빙 | 서버 하나, CORS 없음 |
| LLM | OpenAI GPT-5.6 Terra 단일, 공식 Python SDK 직접 호출 | 의뢰인 결정. 모델 ID는 env `OPENAI_MODEL` |
| 문서 파싱 | 업스테이지 Document Parse (HTML + 블록 위치) | 대회 파트너, 표·말소 표기 보존 |
| 에이전트 루프 | 직접 구현 (판단 → 도구 → 관찰). 프레임워크 없음 | 도구 8개, 과설계 방지 |
| 실시간 | SSE (sse-starlette). 이벤트는 DB에도 저장 | 단방향으로 충분 |
| 외부 데이터 | 공공데이터포털 실거래가·건축HUB·법정동코드, HUG 명단 일일 스냅샷 | RFQ Q26 |
| 컨테이너·배포 | Docker 이미지 1개 + docker compose (app, db). Azure (컴퓨트 종류 미결) | 어디든 올릴 수 있게 |
| 테스트 | pytest (rules·registry 중심), vitest 최소 | 대회 심사에 협업 과정 기록 포함 |

## 절대 규칙

- **계산은 코드, 판단·설명은 에이전트.** `rules` 도메인은 LLM·네트워크를 부르지 않는다. 등급은 코드가 정하고 에이전트는 바꾸지 못한다 (PRD R6).
- **도구 결과 밖의 사실을 말하지 않는다.** 의견서 수치는 후검증으로 도구 출력과 대조한다 (PRD R9·R10).
- **업로드 원본은 디스크에 쓰지 않는다.** 파싱 결과는 세션 만료(24h) 후 삭제. 로그에 이름·주민번호·상세주소 금지 (PRD N1).
- **외부 호출은 5초 타임아웃·재시도 1회·실패 사유 반환.** 실패해도 의견서는 나온다 (PRD N3).
- **한도**: 도구 호출 20회, 질문 5개, 건당 비용 300원, 파일 10MB·20쪽, IP당 일 5건 (예시 파일 제외).
- 키는 env로만. `.env` 커밋 금지.
- 판정 규칙(R5·R6)과 최우선변제금·법정동코드 같은 상수는 출처와 기준일을 코드 주석에 남긴다.

## 도메인 구조 (INFRA 4절)

`review`(세션·루프·이벤트) · `registry`(등기부 파싱) · `rules`(합산·신호·등급, 순수) · `lookup`(시세·대장·명단, 캐시) · `report`(의견서·특약·PDF) · `gate`(파일 검사·해시 캐시·IP 한도). 백엔드 코드는 fastapi-domain-architecture 스킬 규칙을 따른다.

## 도메인 용어

| 용어 | 뜻 |
|---|---|
| 등기부 | 등기사항전부증명서. 표제부(건물 정보)·갑구(소유권)·을구(근저당 등) |
| 채권최고액 | 근저당권이 담보하는 상한 금액. 합산 시 실채무가 아니라 이 값을 쓴다 (LH 방식) |
| 선순위 | 내 보증금보다 먼저 배당받는 권리의 합 (근저당·전세권·다가구 기존 세입자 보증금) |
| 부채비율 | (선순위 + 내 보증금) ÷ 주택 가격. 90% = LH·HUG 불가 경계, 70% = 서울시 권장선 |
| 즉시 위험 신호 | 권리침해·신탁·임차권등기·소유자 불일치·명단 일치·위반건축물 (PRD R5) |
| 확인 못 함 | 도구 실패나 무응답으로 판정하지 못한 항목. 숨기지 않고 의견서에 표시 |
| 예시 파일 | 가상 등기부 PDF 3건 (`assets/samples/`). 실제 에이전트가 실제로 처리한다. 미리 계산된 재생 없음 |

## 브랜치 전략

`main`(운영, Azure가 바라봄) + `dev`(통합) + 작업 브랜치(`feat/…`, `fix/…`). 작업 브랜치는 dev에서 분기해 dev로 squash 머지. dev → main 머지는 배포 시점. 싱크독이 명세를 main에 직접 커밋하므로 dev 작업 전 `git merge main`으로 명세를 가져온다. 보호 브랜치 직접 커밋은 훅이 차단한다.

## 일정 (RFQ 5절)

09-15 파싱·규칙 도구 · 09-16 에이전트 루프·외부 조회·질문 · 09-17 화면 3개·진행 표시·하이라이트 · 09-18 배포·제한·실사용자 테스트 · 09-19 제출 문구·데모 영상·모바일 · 09-20 제출.
